/**
 * CONCEPTS:
 * 1. main function is special: it is always the first code that runs in every executable Rust Program, has no params and returs nothing
 * 2. Function body is wrapped in {}
 * 3. println! calls a Rust macro (!) means macro
 * 4. end line with semicolon (;)
 **/
fn main() {
    println!("Hello, world!");
}
